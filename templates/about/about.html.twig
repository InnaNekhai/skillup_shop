{% extends 'base.html.twig' %}

{% block title %}
    О нас - {{ parent() }}
{% endblock %}

{% block body %}
    <h1 style = "color:darkblue" > Информация о магазине </h1>
    <p>{{ about }}</p>

    <a href="{{ notebookUrl }}" >Ноутбуки</a><br>
    <a href="{{ path('category_show', {'slug':'tvs', 'page':'5'}) }}" >Телевизоры</a> <br>
    <a href="{{ path('category_show', {'slug':'media'}) }}" >Медиацентры</a> <br><br>


    Вы смотрели:  <a href="{{ path('category_show', {'slug':  lastCategory  }) }}" >{{ lastCategory }}</a>
{% endblock %}